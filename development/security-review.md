# Security review

A walk through `development/PLAN.md` §23 against the implementation. First done in Phase 4 (2026-09-22); updated on 2026-09-24 for Phase 5 (SSR, React adapter), the post-1.0 additions (`@swarakaka/bridge-vite`, `useJson`) and the hardening pass. Each row names where the control lives and which test exercises it. Rows marked **added** or **fixed** changed in the review that names them.

| Concern                      | Control                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                             | Where                                                                                               | Evidence                                                                                                                                           |
| ---------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | --------------------------------------------------------------------------------------------------- | -------------------------------------------------------------------------------------------------------------------------------------------------- |
| Stream authentication        | Ordinary guards run before the stream; the fetch transport sends cookies or `Authorization`; native `EventSource` uses cookies or a signed 60 s ticket bound to the user; long-lived tokens never appear in URLs.                                                                                                                                                                                                                                                                                                   | `StreamResponse`, `AuthenticateStreamTicket`, `Bridge::streamTicket()`                              | `StreamResponseTest` (tickets, tampering), `stream-protocol.spec.ts`                                                                               |
| JSON API authentication      | Laravel guards only; Bridge maps `AuthenticationException` to 401 per mode and never bypasses a guard.                                                                                                                                                                                                                                                                                                                                                                                                              | `ErrorMapper`, playground `auth:sanctum` routes                                                     | `ErrorsTest`, `CustomerModesTest::test_guests_are_represented_per_mode`                                                                            |
| CSRF                         | Laravel's cookie/header scheme untouched; `Bridge\Http\Middleware\VerifyCsrfToken` skips only bearer requests without a session cookie; streams are GET and never mutate.                                                                                                                                                                                                                                                                                                                                           | `VerifyCsrfToken`                                                                                   | `CsrfTest`                                                                                                                                         |
| CORS                         | Not handled by Bridge; apps use `HandleCors`. Documented.                                                                                                                                                                                                                                                                                                                                                                                                                                                           | docs                                                                                                | —                                                                                                                                                  |
| Open redirects               | `X-Bridge-Location` only from app-controlled redirects; targets containing `\`, control characters or whitespace, or that `parse_url` rejects, count as external (browsers resolve `/\evil.com` to another host) (**fixed**, hardening); the client refuses `navigate` events and visits to other origins unless `allowExternalNavigate`.                                                                                                                                                                           | `PageRepresenter::isSameOrigin`, `Router.navigate`, `StreamClient.handleControl`                    | `RedirectsTest` (treats urls that browsers resolve to another host as external), `stream.test.ts` (navigate to evil origin ignored)                |
| Cache poisoning              | Page/JSON default `private, no-cache`; `Vary` on every negotiation header; `public` refused for authenticated users unless forced.                                                                                                                                                                                                                                                                                                                                                                                  | `HandleBridgeRequests`, `CacheOptions`                                                              | `CachingAndBuildTest`                                                                                                                              |
| Private data caching         | Embedded shell `private, no-store` for authenticated users; client page cache cleared on mutations and 401/403/409.                                                                                                                                                                                                                                                                                                                                                                                                 | `HtmlRepresenter`, `Router`                                                                         | `CachingAndBuildTest`, `router.test.ts` (cache cleared after mutation)                                                                             |
| Event leakage                | Invalidate-first; channels computed server-side; client-requested channels must pass `Bridge::channel()`; unauthorized requests end with `error{403, final}`.                                                                                                                                                                                                                                                                                                                                                       | `StreamResponse`, `ChannelAuthorizer`                                                               | `StreamResponseTest` (authorizes client-requested channels)                                                                                        |
| Tenant isolation             | Channel patterns with authorizers; bus keys prefixed per app (`bridge.stream.prefix`).                                                                                                                                                                                                                                                                                                                                                                                                                              | `ChannelAuthorizer`, bus drivers                                                                    | `CursorAndBusTest`                                                                                                                                 |
| Replay                       | Every (re)connection is a new authenticated request; replayed events are re-filtered by the connection's channels; replayed `end` signals are ignored; ids grant nothing. Buses that implement `ReplayWindow` refuse ids they cannot honour (foreign, future, pruned or trimmed) and the stream answers `replayed: false`, so the client resyncs instead of silently missing events (**fixed**, hardening). The `lastEventId` query parameter (EventSource) is outside ticket signatures because ids grant nothing. | `StreamResponse::subscribe`, `ReplayWindow`, database and Redis buses                               | `StreamResponseTest` (replay, historical end, replayed false, `lastEventId` query), `DatabaseBusTest`, `RedisStreamsBusTest` (trimming, retention) |
| Connection exhaustion        | Bounded `max_duration`; per-user concurrent cap (`ConnectionLimiter`, lease-based under a cache lock where supported) released even on client abort; `throttle:bridge-stream` limiter (Phase 4); a capacity refusal sends `error{429, final:false}` and the client resets its backoff only after a healthy connection.                                                                                                                                                                                              | `StreamResponse`, `ConnectionLimiter`, `BridgeServiceProvider::registerRateLimiter`, `StreamClient` | `StreamResponseTest` (limits), `stream.test.ts`                                                                                                    |
| Unbounded client input       | `?channels=` capped to 20 names of ≤ 190 chars (**added this phase**); partial-selection headers only select existing keys.                                                                                                                                                                                                                                                                                                                                                                                         | `StreamResponse::requestedChannels`, `PartialSelection`                                             | `PropsTest`                                                                                                                                        |
| Deserialization              | Client validates `Content-Type` and `type` before applying a response; unknown control types and kinds are ignored.                                                                                                                                                                                                                                                                                                                                                                                                 | `responseParser`, `StreamClient`                                                                    | `responseParser.test.ts`                                                                                                                           |
| Debug leakage                | Traces only in JSON mode with `app.debug`; page and stream errors never carry traces.                                                                                                                                                                                                                                                                                                                                                                                                                               | `ErrorMapper`, `ErrorEnvelope`                                                                      | `ErrorsTest` (debug details only in JSON)                                                                                                          |
| Signed ticket scope          | Tickets carry `bridge_user` and route parameters in the signature and expire after `ticket_ttl_s`; the middleware authenticates for one request and runs before `auth:*`; token and request guards are supported through the user provider (hardening).                                                                                                                                                                                                                                                             | `AuthenticateStreamTicket`, provider priority insertion                                             | `StreamResponseTest` (tampered ticket, guards without `onceUsingId`)                                                                               |
| Rate limiting of mutations   | Application concern (`throttle`); Bridge adds nothing that bypasses it.                                                                                                                                                                                                                                                                                                                                                                                                                                             | —                                                                                                   | —                                                                                                                                                  |
| XSS: embedded page object    | The page object is embedded in `<script type="application/json">` with `JSON_HEX_TAG`, `JSON_HEX_AMP`, `JSON_HEX_APOS` and `JSON_HEX_QUOT`, so no prop can close the element or open a comment; the root id is `htmlspecialchars`-escaped. The Blade directives (`Shell::root`) and `<x-bridge::app />` use the same encoder.                                                                                                                                                                                       | `HtmlRepresenter::encodeEmbedded`, `Shell::root`                                                    | `ModesTest` (escapes props so they cannot close the embedded script element, **added**)                                                            |
| XSS: head tags               | `renderHead` escapes the title and attribute values; attribute names outside `[A-Za-z_:][A-Za-z0-9_:.-]*`, and the `data-bridge-head` marker, are dropped on the server and by the client `HeadManager` (**fixed** in this update: names were written verbatim, so a meta map with keys from user data could inject markup into `<head>`).                                                                                                                                                                          | `core/src/head.ts` (shared by the Vue and React `BridgeHead`)                                       | `head.test.ts`                                                                                                                                     |
| SSR trust boundary           | The gateway inserts the SSR server's `head` and `body` verbatim; it only checks the payload shape (`body` a string, `head` a list of strings). The Node server binds `127.0.0.1` by default, has no authentication, accepts only `POST /render` with a valid page object and `GET /health`.                                                                                                                                                                                                                         | `HttpSsrGateway`, `createSsrServer`                                                                 | `SsrTest` (invalid payload falls back), `vue/tests/server.test.ts` (rejects bad input)                                                             |
| SSR request isolation        | Each render creates its own Bridge instance with `global: false` and its own app, so `getBridge()`/`router` never point at another request; the Vue state mirror is a `WeakMap` keyed by instance. Module-level state in application pages is not isolated (documented).                                                                                                                                                                                                                                            | `createSsrRenderer` (Vue, React), `createBridge`                                                    | `p2.test.ts` (does not replace the global bridge instance), `parity.test.tsx`                                                                      |
| SSR data in transit          | The gateway POSTs every prop of the page (including the user's data) to the SSR server over plain HTTP; loopback by default. Docs require a private network or TLS otherwise, and `NO_PROXY` when `http_proxy` is set.                                                                                                                                                                                                                                                                                              | `HttpSsrGateway`, docs (SSR → Security)                                                             | —                                                                                                                                                  |
| SSR availability             | Request bodies over `maxBody` (2 MB) get 413 and the page renders on the client (a non-2xx status, so no cooldown); failures fall back to client rendering; an unreachable or timed-out server is skipped for `ssr.cooldown_s`. Only embedded shells are server-rendered.                                                                                                                                                                                                                                           | `HttpSsrGateway`, `createSsrServer`                                                                 | `SsrTest` (fallback, cooldown, error status keeps trying, static shells)                                                                           |
| Shared props across requests | Props shared after boot are dropped on `RequestHandled`, so Octane workers cannot carry one user's shared props into the next request (hardening).                                                                                                                                                                                                                                                                                                                                                                  | `Bridge` shared props, provider                                                                     | `PropsTest` (drops props shared during a request once it is handled)                                                                               |
| EventSource refusals         | `EventSource` hides the status of a refused connection; the client probes the same URL once with `Accept: application/json`, which auth middleware answers with 401/403 before the stream route (an allowed request gets 406, so the probe never opens a stream or takes a slot). 401/403 stop reconnecting.                                                                                                                                                                                                        | `StreamClient` EventSource transport                                                                | `transports.test.ts` (refused connection turns out to be a 401)                                                                                    |
| JSON client (`useJson`)      | Same routes and guards as JSON mode; mutations send the XSRF header and clear the page cache; `401`/`419` are reported to the caller, never turned into reloads or redirects.                                                                                                                                                                                                                                                                                                                                       | `JsonClient`, `JsonRequest`, `useJson`                                                              | `json.test.ts` (XSRF token, error kinds)                                                                                                           |
| Vite plugin                  | Build-time transform only: rewrites `createBridgeApp`/`createSsrRenderer` calls into an `import.meta.glob` over the calling file's `./Pages`; nothing runs in the browser or on the server at request time.                                                                                                                                                                                                                                                                                                         | `packages/vite`                                                                                     | `packages/vite/tests`                                                                                                                              |

## Residual risks and recommendations

- **CDNs that ignore `Vary`** can serve a JSON representation to a browser for public pages. Keep public caching opt-in (default) and key CDN caches on `Accept` when enabling it.
- **Data-carrying stream events** (`prop`, application events) bypass request-time authorization. Publish them only to per-user or per-tenant channels; use `invalidate` for anything else. The playground follows this rule.
- **Ticket URLs in logs.** Tickets expire in 60 s, but access logs still record them. Treat log access as sensitive or shorten `ticket_ttl_s`.
- **Database bus growth.** Schedule `bridge:stream:prune`.
- The Redis driver's cross-channel replay is approximate at millisecond granularity (documented in PLAN §20.2 notes).
- **The SSR server is trusted and unauthenticated.** Anyone who can reach it can render pages, and whatever answers on `bridge.ssr.url` controls the HTML. Keep it on loopback or a private network; never bind a public interface with `host`.
- **Module-level state in application pages** is shared by every request the SSR process serves. Per-user data belongs in props or in state created during setup (documented in SSR → Security).
- **`v-html` / `dangerouslySetInnerHTML`** are as dangerous during SSR as in the browser; Bridge does not sanitize props.
- **No automated security scanning in CI.** `composer audit`, `pnpm audit` and a code scanner (CodeQL) are not run; recommended before 1.0 is announced widely.
