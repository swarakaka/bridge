{
  "name": "bridge-e2e",
  "private": true,
  "version": "0.0.0",
  "description": "Playwright end-to-end suite against the playground",
  "type": "module",
  "scripts": {
    "test": "playwright test",
    "test:headed": "playwright test --headed",
    "report": "playwright show-report"
  },
  "devDependencies": {
    "@playwright/test": "^1.55.0",
    "@types/node": "^22.0.0",
    "typescript": "^5.8.0"
  }
}
