# Documentation

- `propmts.md` — the original design brief (filename typo preserved deliberately; it is referenced elsewhere).
- `PLAN.md` — the technical implementation plan. Architectural source of truth.
- The VitePress documentation site is added in Phase 4 (`docs/PLAN.md` §32). The normative protocol spec lives in `packages/protocol/spec`.
