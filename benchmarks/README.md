# Benchmarks

Reproducible benchmarks comparing an Inertia-style page, Bridge Page, Bridge JSON and Bridge SSE. Scenarios, metrics and tooling are defined in `docs/PLAN.md` §25. Implemented in Phase 4.

No performance claim may appear in the documentation without an entry in `RESULTS.md` produced by `run.sh`.
