---
'@swarakaka/bridge-protocol': major
'@swarakaka/bridge-core': major
'@swarakaka/bridge-vue': major
---

1.0: server-side rendering (`@swarakaka/bridge-vue/server` renderer and HTTP server, hydration, `BridgeHead` head fragments, `data-bridge-hydrated` marker), ESM output with explicit `.js` specifiers so Node consumers work, `useStream` no longer connects during SSR.
